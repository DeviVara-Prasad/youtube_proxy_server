{
  "name": "youtube-proxy-server",
  "version": "1.0.0",
  "description": "A Node js web server that fetches data from a third party API and exposes it to a client App.",
  "main": "server.js",
  "scripts": {
    "test": "test",
    "start": "node server.js",
    "build": "parcel build server.js --target node --out-dir dist"
  },
  "author": "Devi Vara Prasad Nulu",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "cross-fetch": "^4.0.0",
    "express": "^4.18.3"
  },
  "devDependencies": {
    "parcel-bundler": "^1.12.5"
  }
}
